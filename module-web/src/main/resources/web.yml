server:
  port: 8080
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

management:
  server:
    port: 8081
  endpoints:
    web:
      exposure:
        include: "health,info"